# งานที่ต้องทำ (To-Do)

- [x] (Phase 2.1) พัฒนา `normalize_thai` ด้วย Regex ใน `core/src/search.rs` เพื่อลบสระ/วรรณยุกต์ไทยตอนค้นหา
- [x] (Phase 2.1) เขียน Test Case ตรวจสอบการค้นหาภาษาไทยแบบไม่คำนึงถึงวรรณยุกต์
- [x] (Phase 3.2) พัฒนาหน้า Admin Dashboard ด้วย Next.js ที่ `/app/page.tsx`
- [x] (Feature) Add custom rule sets support via YAML/JSON configuration
- [x] (Feature) Add CLI `scan` command for directory validation with ignore patterns (glob syntax)
- [x] (Docs) Add CI/CD Integration Guide for GitHub Actions, GitLab CI, and Jenkins
